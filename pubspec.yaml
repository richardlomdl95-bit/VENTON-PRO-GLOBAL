name: venton_pro
description: VENTON PRO - Red social y publicidad para negocios.
publish_to: 'none'
version: 2.2.0+22

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.3.0
  cached_network_image: ^3.4.1
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  video_player: ^2.9.2
  share_plus: ^10.0.2
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.4.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  youtube_player_flutter: ^9.1.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
